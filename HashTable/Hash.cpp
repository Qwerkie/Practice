#include <cstdlib>
#include <iostream>
#include <string>
#include "Hash.h"

using namespace std;

int hash::Hash(string key){
    int hash = 0;
    int index;
    
    index = key.length();
    
    return index;
}
